/** Индекс сканера (клиентская часть). */

export { parseAgentConfig } from "./parse-config"
export { buildArchitectureGraph, categoryLabel } from "./build-graph"
export { buildLocalFallbackGraph } from "./local-fallback"
