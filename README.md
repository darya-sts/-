# MarvinBot

Проект Telegram-бота для работы с Cursor через MCP-сервер `cursor-telegram-mcp`.

Конфигурация MCP находится в `.cursor/mcp.json`. После изменения файла перезагрузите окно Cursor (Command Palette → **Developer: Reload Window**), чтобы сервер Telegram подключился.
